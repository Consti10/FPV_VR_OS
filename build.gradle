// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    
    repositories {
        google()
        jcenter()
        maven { url 'https://maven.fabric.io/public' }
        maven {
            url 'https://maven.google.com/'
            name 'Google'
        }
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.5.0'
        classpath 'com.google.gms:google-services:4.3.1'
        classpath 'io.fabric.tools:gradle:1.28.1'
        classpath 'com.github.dcendents:android-maven-gradle-plugin:2.1'
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        maven { url "https://jitpack.io" } //this one is for being able to include projects from github
        maven { url 'https://maven.fabric.io/public' }
    }

    ext {
        minSdkVersion = 21
        compileSdkVersion = 28
        targetSdkVersion = 28
        appcompatVersion = '1.1.0'
        materialVersion = '1.1.0-alpha10'
        constraintlayoutVersion='1.1.3'
        mGvrVersion = '1.190.0'
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

task myTaskDoSomething()  {
    println 'Hello TEST'
    //commandLine "git", "clone https://github.com/Consti10/LiveVideo10ms"
    //from zipTree("${project.rootDir}/libraries/sdk-base-1.190.0.aar")
    //into "${project.rootDir}/libraries/"
    //include "headers/vr/gvr/capi/**/*h"
    //include "jni/**/libgvr_audio.so"
    //include "jni/**/libgvr.so"
}
